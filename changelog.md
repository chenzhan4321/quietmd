# changelog

## [2026-08-20] 0.3.0: 重写打印模块

原来的打印样式只有 16 行：刷白底、藏工具栏，然后一句 `article{max-width:none}` 把行宽
限制整个取消掉 —— 屏幕上 44rem 的版心到了纸上变成横跨整页，A4 一行 45 个汉字，印出来像
txt。这一版把打印当成第二次排版来做。

- **版心**：新增 `@page{margin:19mm 32mm 21mm}`，行宽由页边距定（A4 一行约 36 汉字 /
  72 西文字符），正文居中、左右留白对称。不写 `size`，Letter 一样合用。
- **打印样式表独立成 `PRINT_CSS`，排在风格表之后**。原来它写在基础表里，而风格的选择器
  带 `[data-style]`，同特异性下后写的赢 —— 也就是说 book 的米黄底、medium 的字号从来
  就没被打印样式压住过。
- **字号与行距在纸上统一**：11.5pt / 行距 1.52（稿件风格保留双倍行距）。风格们写的
  `calc(var(--fs) + 3px)` 是按 18px 屏幕基准调的，直接搬到 pt 上会让一行在 30–40 字
  之间乱跳，版心就白算了。
- **分页**：孤行寡行各留三行；标题不落页底；代码块、表格行、公式、图片、对照页不跨页
  断开；表格跨页时表头重复；图片限高一页。
- **不再被无声裁掉**：屏幕上横向滚动的地方（长代码行、宽表格、宽公式、跑代码的输出）在
  纸上改成折行或缩到版心宽。超长公式原本靠右缘渐隐提示"还能往右滚"，在纸上等于把后半条
  擦掉，现在整条缩放。
- **不依赖背景色**（浏览器默认不打印背景）：代码块细边框、表格三线表、`<mark>` 单独开
  `print-color-adjust:exact`。
- **颜色不作为唯一信息通道**：版本对比在纸上用 `−`/`+` 加实线/双线区分，不靠红绿。
- **链接**：黑字加下划线；站外链接在后面用小字附上地址。
- 修 `tufte` 风格的 `main{padding-left:3rem}` 带到纸上造成的版心偏移。
- 顺手修一个既有 bug：`.footnotes hr{display:none}` 从来没命中过（`<hr class=
  "footnotes-sep">` 是 `.footnotes` 的兄弟不是子元素），脚注上方一直画着两条线。
- 重构：行距改由 `--lh` 变量承载，八种风格不再各自写死 `body{line-height}`。
