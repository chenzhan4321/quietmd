# 体检测试

定义宏：$$\newcommand{\Used}{\mathbb{U}}\newcommand{\NeverUsed}{\mathbb{N}}$$

用一次：$\Used^2$

\begin{equation}
E = mc^2 \label{eq:good}
\end{equation}

\begin{equation}
a = b \label{eq:dup}
\end{equation}

\begin{equation}
c = d \label{eq:dup}
\end{equation}

引用存在的：\eqref{eq:good}，引用不存在的：\eqref{eq:missing}
